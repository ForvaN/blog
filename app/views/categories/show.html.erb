<%= render "shared/breadcrumb", category: @category %>
<h1><%= @category.name %></h1>
<section id="posts">
  <%= render @posts %>
</section>
<div class="digg_pagination">
  <%= will_paginate @posts %>
</div>