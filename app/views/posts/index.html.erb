<h1><%= t(".title") %></h1>
<% if user_signed_in? && current_user.admin? %>
  <%= link_to "Nowy artykuł", new_admin_post_path,
    class: "btn btn-outline-primary float-right" %>
<% end %>


<section id="posts">
  <%= render @posts %>
</section>
<div class="digg_pagination">
  <%= will_paginate @posts %>
</div>